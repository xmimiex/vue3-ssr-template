export type Routes = 'home' | 'product' | 'category' | 'notFound' | 'countryLanguage'

export enum ROUTES {
  HOME = 'home',
  PRODUCT = 'product',
  CATEGORY = 'category',
  NOTFOUND = 'notFound',
  COUNTRY_LANGUAGE = 'countryLanguage',
}
